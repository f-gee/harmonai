{
  "name": "harmonai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run web -w frontend\"",
    "dev:mobile": "concurrently -n backend,frontend -c blue,green \"npm run dev -w backend\" \"npm run start -w frontend\"",
    "backend": "npm run dev -w backend",
    "frontend": "npm run web -w frontend",
    "backup": "npm run backup -w backend",
    "restore": "npm run restore -w backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
